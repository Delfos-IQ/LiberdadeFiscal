# Icons — placeholders pendientes

Este directorio está vacío a propósito. `manifest.json` ya referencia:

- `icon-192.png` (192×192, `purpose: "any"`)
- `icon-512.png` (512×512, `purpose: "any"`)
- `icon-maskable.png` (512×512, `purpose: "maskable"`, con margen de
  seguridad ~20% para el área "safe zone" de iconos maskable)

Sube aquí los ficheros finales (logo: pájaro estilizado + barras de
crecimiento) generados por el autor. Hasta entonces, la PWA no pasará
la validación completa de iconos en Lighthouse/DevTools > Application >
Manifest, pero el resto del shell funciona con normalidad.
